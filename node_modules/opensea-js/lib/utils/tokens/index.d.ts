import { Chain } from "../../types";
interface Token {
    name: string;
    symbol: string;
    decimals: number;
    address: string;
}
export interface NetworkTokens {
    canonicalWrappedEther: Token;
    otherTokens: Token[];
}
export declare const getCanonicalWrappedEther: (chain: Chain) => Token;
export {};
